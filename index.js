const array = ["Lodash", "React", "Next", "Strapi", "Axios", "Typescript"]

// array.forEach(function (item) {
//     console.log(item);
//   });

// array.reverse().forEach(function (item) {
//     console.log(item);
//   });

// array.sort().forEach(function (item) {
// console.log(item);
// });

// const car = {
// brand: "Fiat",
// model: "500",
// year: 50,
// };

// let { brand, model, year } = car;

// console.log(brand, model, year);

let rnd = Math.random() * 555;
console.log(`Nahodne cislo je: ${rnd}`)